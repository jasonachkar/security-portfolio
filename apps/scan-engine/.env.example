PORT=4000
NODE_ENV=development
# Optional: GreyNoise API key for higher rate limits (free tier works without it)
GREYNOISE_API_KEY=
# Optional: NVD API key for higher rate limits (free tier works without it)
NVD_API_KEY=
