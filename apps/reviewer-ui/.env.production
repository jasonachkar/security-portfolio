# On Vercel the frontend and scan-engine share the same domain, so the relative
# /api proxy routes (see vercel.json) handle API calls. Leave this empty unless
# you host the scan-engine on a separate origin.
VITE_API_BASE_URL=
