[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "threat-intel-service"
version = "0.1.0"
description = "Threat intelligence service with CVE, MITRE ATT&CK, and IOC tracking"
requires-python = ">=3.12"
license = { text = "MIT" }

dependencies = [
    "fastapi==0.115.8",
    "uvicorn==0.30.6",
    "pydantic==2.10.5",
    "pydantic-settings==2.7.1",
    "sqlalchemy==2.0.36",
    "psycopg==3.2.3",
    "alembic==1.14.0",
    "httpx==0.27.2",
    "celery==5.4.0",
    "redis==5.2.0",
    "tenacity==9.0.0",
    "structlog==24.4.0",
    "python-json-logger==2.0.7",
    "shared-security-core @ file:///app/shared-security-core",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.4",
    "pytest-asyncio==0.25.2",
    "black==24.10.0",
    "ruff==0.8.5",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/threat_intel"]
