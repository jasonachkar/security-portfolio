# Workflow Evidence

Place exported or uploaded workflow summaries here when preparing portfolio screenshots.

Do not commit logs containing bearer tokens, Azure identifiers that should remain private, or deployment credentials.
